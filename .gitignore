node_modules/
dist/
.wrangler/
.dev.vars
.dev.vars.local
# wrangler.toml is generated from wrangler.toml.example by the setup script
# (contains workspace-specific IDs). Committed template is wrangler.toml.example.
wrangler.toml
.env
.env.local
.DS_Store
.claude/
*.log

# Sentry Config File
.sentryclirc

# Conductor working state (state is not committed)
conductor/setup_state.json
conductor/**/*_state.json

# Track specs are local-only working docs — public repo, keep design/roadmap
# and any internal (client/infra) references out of git.
conductor/tracks/
